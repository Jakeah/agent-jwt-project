<?xml version="1.0" encoding="UTF-8"?>
<!--
  Auth for the verified-coach live-game pull (Apex ChessCoachGetLiveGame → Rails /coach/game_state).
  Custom protocol with a NamedPrincipal that injects a static "Authorization: Bearer <token>" header.
  The token VALUE is NOT in metadata — it's set on the principal's auth parameter in Setup after
  deploy (External Credentials → ChessLiveGame → Principals → edit), matching Rails' COACH_PULL_TOKEN
  config var. Keeping the secret out of the repo, like IDENTITY_JWT_PRIVATE_KEY / the ECA secret.
-->
<ExternalCredential xmlns="http://soap.sforce.com/2006/04/metadata">
    <authenticationProtocol>Custom</authenticationProtocol>
    <externalCredentialParameters>
        <parameterGroup>DefaultGroup</parameterGroup>
        <parameterName>Custom</parameterName>
        <parameterType>AuthProtocolVariant</parameterType>
        <parameterValue>NoAuthentication</parameterValue>
    </externalCredentialParameters>
    <externalCredentialParameters>
        <parameterGroup>ChessLiveGamePrincipal</parameterGroup>
        <parameterName>ChessLiveGamePrincipal</parameterName>
        <parameterType>NamedPrincipal</parameterType>
        <sequenceNumber>1</sequenceNumber>
    </externalCredentialParameters>
    <label>ChessLiveGame</label>
</ExternalCredential>
